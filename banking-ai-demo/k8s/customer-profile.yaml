apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-profile-service
  namespace: finance-demo
  labels:
    app: customer-profile-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-profile-service
  template:
    metadata:
      labels:
        app: customer-profile-service
    spec:
      imagePullSecrets:
        - name: vcr-cred
      containers:
        - name: customer-profile-service
          # CI thay :latest bằng commit SHA (xem README).
          image: vcr.vngcloud.vn/114544-ea-hackathon/customer-profile-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: customer-profile-service
  namespace: finance-demo
  labels:
    app: customer-profile-service
spec:
  type: ClusterIP
  selector:
    app: customer-profile-service
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
